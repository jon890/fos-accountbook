# =====================================================
# 환경 변수 예시 (Family Budget Frontend)
# =====================================================
# 이 파일을 복사하여 .env.local 파일을 생성하세요
# cp .env.example .env.local
# =====================================================

# =====================================================
# NextAuth 설정
# =====================================================
# 애플리케이션 URL
# 로컬: http://localhost:3000
# 프로덕션: https://your-domain.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# NextAuth Secret (최소 32자 이상의 랜덤 문자열)
# 생성 방법: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-change-in-production-must-be-at-least-32-characters"

# =====================================================
# Google OAuth 설정
# =====================================================
# Google Cloud Console에서 발급받은 OAuth 2.0 클라이언트 ID
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

NAVER_CLIENT_ID="your-navert-client-id"
NAVER_CLIENT_SECRET="your-naver-client-secret"

# =====================================================
# 백엔드 API 설정
# =====================================================
# 클라이언트에서 접근 가능한 API URL (NEXT_PUBLIC_ 접두사 필수)
NEXT_PUBLIC_API_BASE_URL="http://localhost:8080/api/v1"

# 서버 사이드 전용 API URL (Server Components, Server Actions)
BACKEND_API_URL="http://localhost:8080/api/v1"

# =====================================================
# 프로덕션 환경 설정 예시
# =====================================================
# NEXTAUTH_URL="https://your-domain.vercel.app"
# NEXTAUTH_SECRET="production-secret-generate-with-openssl-rand-base64-32"
# NEXT_PUBLIC_API_BASE_URL="https://api.your-domain.com/api/v1"
# BACKEND_API_URL="https://api.your-domain.com/api/v1"
