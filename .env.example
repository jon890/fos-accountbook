# =====================================================
# 프론트엔드 환경 변수 예시
# =====================================================
# 이 파일을 복사하여 .env.local 파일을 생성하세요
# cp .env.example .env.local
# =====================================================

# =====================================================
# 데이터베이스 (MySQL - 백엔드와 동일한 DB 사용)
# =====================================================
# NextAuth 인증 테이블만 사용
DATABASE_URL="mysql://accountbook_user:accountbook_password@localhost:3306/accountbook"

# =====================================================
# NextAuth 설정
# =====================================================
# 로컬 개발
NEXTAUTH_URL="http://localhost:3000"
# 프로덕션 배포 시: https://your-domain.vercel.app

# NextAuth Secret (랜덤 문자열로 변경 필수!)
# 생성 방법: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-change-in-production"

# =====================================================
# Google OAuth 설정
# =====================================================
# Google Cloud Console에서 발급받은 값
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# =====================================================
# 백엔드 API 설정
# =====================================================
# 클라이언트 사이드에서도 접근 가능 (NEXT_PUBLIC_ 접두사)
NEXT_PUBLIC_API_BASE_URL="http://localhost:8080/api/v1"

# 서버 사이드 전용 (Server Actions, API Routes)
BACKEND_API_URL="http://localhost:8080/api/v1"

# =====================================================
# 프로덕션 환경 설정 예시
# =====================================================
# DATABASE_URL="mysql://user:password@production-host:3306/accountbook"
# NEXTAUTH_URL="https://your-domain.vercel.app"
# NEXTAUTH_SECRET="production-secret-generate-random-string"
# NEXT_PUBLIC_API_BASE_URL="https://api.your-domain.com/api/v1"
# BACKEND_API_URL="https://api.your-domain.com/api/v1"

# =====================================================
# 선택적 설정
# =====================================================
# 로그 레벨
# LOG_LEVEL="debug"

# 개발 모드
# NODE_ENV="development"
