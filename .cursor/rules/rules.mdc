---
alwaysApply: true
description: 우리집 가계부 프로젝트의 전체 개발 규칙 및 컨벤션
version: "2.0"
---

# 우리집 가계부 - Cursor AI 개발 규칙

## 🎯 프로젝트 개요

**프론트엔드**: Next.js 15 + TypeScript + Tailwind CSS
**백엔드**: Spring Boot + JPA + MySQL
**인증**: NextAuth.js (Google OAuth)
**배포**: Vercel (Frontend) + Railway (Backend)

---

## 📋 코딩 컨벤션

### 1. 프론트엔드 (Next.js)

#### TypeScript 규칙

- ❌ **절대 `any` 타입 사용 금지** - `unknown` 또는 정확한 타입 사용
- ✅ **Zod 스키마 필수 사용**: 모든 폼 데이터 및 API 입력 검증
- ✅ Zod의 `safeParse()` 사용
- ✅ 에러는 `error.flatten().fieldErrors` 사용

#### 타입 구조

- **도메인별 타입 분리**: `src/types/actions/` 폴더에 도메인별로 구조화
  - `common.ts` - 공통 타입
  - `dashboard.ts` - 대시보드 타입
  - `expense.ts` - 지출 타입
  - `family.ts` - 가족 타입
  - `invitation.ts` - 초대 타입
  - `index.ts` - 통합 export

#### Next.js 15 아키텍처

- **Server Components 우선**: 기본적으로 모든 컴포넌트는 Server Component
- **Server Actions 우선**: 데이터 변경은 Server Actions 사용
- **API Routes 최소화**: 다음 경우에만 사용
  - 외부 Webhook 처리
  - 써드파티 API 통합
  - NextAuth.js 등 라이브러리 요구사항
- **Client Component는 필요시만**: 다음 경우에만 'use client'
  - 사용자 상호작용 (onClick, onChange)
  - React Hooks (useState, useEffect)
  - 브라우저 전용 API

#### React 19 규칙

```typescript
// ❌ Deprecated
import { useFormState } from "react-dom";

// ✅ React 19
import { useActionState } from "react";
const [state, formAction] = useActionState(action, initialState);
```

#### 폼 제출 패턴

```typescript
// ✅ SubmitButton 사용 (중복 제출 방지)
<form action={formAction}>
  <SubmitButton pendingText="저장 중...">저장하기</SubmitButton>
</form>
```

### 2. 백엔드 (Spring Boot)

#### 아키텍처 패턴

- **Controller → Service → Repository** 계층 구조
- **Entity는 snake_case**: 모든 테이블명과 컬럼명은 snake_case
  - 예: `family_members`, `created_at`, `user_id`, `family_uuid`
- **UUID 기반 관계**: 모든 테이블 간 조인은 UUID로 수행
- **Soft Delete**: 모든 비즈니스 엔티티에 `deleted_at` 컬럼

#### API 규칙

- **RESTful API 설계**: HTTP 메서드를 올바르게 사용
  - GET: 조회
  - POST: 생성
  - PUT: 전체 수정
  - PATCH: 부분 수정
  - DELETE: 삭제
- **DTO 패턴**: Request/Response용 별도 DTO 클래스
- **공통 응답 형식**: `ApiSuccessResponse<T>`, `ApiErrorResponse`
- **에러 처리**: `@ControllerAdvice`로 전역 예외 처리

---

## 🛠 기술 스택

### Frontend

- **Framework**: Next.js 15 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS v3
- **UI Components**: shadcn/ui
- **State Management**: React 19 Hooks
- **Form Validation**: Zod
- **Package Manager**: pnpm

### Backend

- **Framework**: Spring Boot 3.x
- **Language**: Java 17+
- **ORM**: JPA (Hibernate)
- **Database**: MySQL
- **Migration**: Flyway
- **API Documentation**: Swagger/OpenAPI
- **Build Tool**: Gradle

### Deployment

- **Frontend**: Vercel
- **Backend**: Railway
- **Authentication**: NextAuth.js (Google OAuth)

---

## 📁 프로젝트 구조

### Frontend (Next.js)

```
src/
├── app/                    # Next.js App Router
│   ├── actions/           # Server Actions (도메인별)
│   │   ├── expense/
│   │   ├── family/
│   │   └── dashboard/
│   ├── api/               # API Routes (최소화)
│   └── (pages)/           # Page Components
├── components/            # React Components
│   ├── ui/               # shadcn/ui Components
│   ├── common/           # 공통 컴포넌트
│   └── [domain]/         # 도메인별 컴포넌트
├── lib/                  # 유틸리티 함수
│   ├── server/          # 서버 전용 유틸
│   └── client/          # 클라이언트 유틸
└── types/               # TypeScript 타입 정의
    └── actions/         # 도메인별 타입
```

### Backend (Spring Boot)

```
src/main/java/com/bifos/accountbook/
├── presentation/         # Controller 레이어
│   └── controller/
├── application/         # Service & DTO 레이어
│   ├── service/
│   └── dto/
├── domain/              # Domain 레이어
│   ├── entity/
│   └── repository/
└── infra/               # Infrastructure 레이어
    └── persistence/
```

---

## 🚀 개발 가이드라인

### 1. Server Actions 패턴 (프론트엔드)

```typescript
// src/app/actions/expense/create-expense-action.ts
"use server";

import { z } from "zod";
import { requireAuth } from "@/lib/server/auth-helpers";
import { serverApiClient } from "@/lib/server/api/client";
import { revalidatePath } from "next/cache";

// Zod 스키마 정의
const createExpenseSchema = z.object({
  amount: z.number().positive("금액은 0보다 커야 합니다"),
  categoryId: z.string().min(1, "카테고리를 선택해주세요"),
  description: z.string().optional(),
  date: z.string().optional(),
});

export async function createExpenseAction(
  prevState: FormState,
  formData: FormData
): Promise<FormState> {
  try {
    // 1. 인증 확인
    await requireAuth();

    // 2. 데이터 추출 및 변환
    const rawData = {
      amount: Number(formData.get("amount")),
      categoryId: formData.get("categoryId")?.toString(),
      description: formData.get("description")?.toString(),
      date: formData.get("date")?.toString(),
    };

    // 3. Zod 검증
    const validated = createExpenseSchema.safeParse(rawData);
    if (!validated.success) {
      return {
        success: false,
        errors: validated.error.flatten().fieldErrors,
        message: "입력값을 확인해주세요",
      };
    }

    // 4. 백엔드 API 호출
    await serverApiClient("/expenses", {
      method: "POST",
      body: JSON.stringify(validated.data),
    });

    // 5. 페이지 재검증
    revalidatePath("/expenses");

    return { success: true, message: "지출이 추가되었습니다" };
  } catch (error) {
    return {
      success: false,
      message: "지출 추가에 실패했습니다",
    };
  }
}
```

### 2. 컴포넌트 개발

```typescript
// Server Component (기본)
export default async function ExpensePage() {
  const expenses = await getExpenses();
  return <ExpenseList expenses={expenses} />;
}

// Client Component (상호작용 필요시)
("use client");
export function ExpenseForm() {
  const [state, formAction] = useActionState(createExpenseAction, initialState);

  return (
    <form action={formAction}>
      {/* 폼 필드 */}
      <SubmitButton pendingText="추가 중...">지출 추가</SubmitButton>
    </form>
  );
}
```

### 3. 백엔드 API 개발

```java
// Controller
@RestController
@RequestMapping("/families/{familyUuid}/expenses")
public class ExpenseController {

    @PostMapping
    public ResponseEntity<ApiSuccessResponse<ExpenseResponse>> createExpense(
        @LoginUser LoginUserDto user,
        @PathVariable String familyUuid,
        @Valid @RequestBody CreateExpenseRequest request
    ) {
        ExpenseResponse response = expenseService.createExpense(
            user.getUserUuid(),
            familyUuid,
            request
        );

        return ResponseEntity
            .status(HttpStatus.CREATED)
            .body(ApiSuccessResponse.of("지출이 등록되었습니다", response));
    }
}

// Service
@Service
@Transactional
public class ExpenseService {
    public ExpenseResponse createExpense(
        CustomUuid userUuid,
        String familyUuid,
        CreateExpenseRequest request
    ) {
        // 비즈니스 로직
        // 검증, 변환, 저장
        return ExpenseResponse.from(expense);
    }
}
```

### 4. 데이터베이스 스키마 변경

#### Backend - Flyway 마이그레이션

```sql
-- src/main/resources/db/migration/V1__init.sql
CREATE TABLE expenses (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    uuid VARCHAR(36) NOT NULL UNIQUE,
    family_uuid VARCHAR(36) NOT NULL,
    category_uuid VARCHAR(36) NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    description VARCHAR(1000),
    date DATETIME NOT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME,
    deleted_at DATETIME,
    INDEX idx_family_uuid (family_uuid),
    INDEX idx_deleted_at (deleted_at)
);
```

**마이그레이션 프로세스:**

1. Entity 클래스 수정
2. Flyway SQL 파일 생성 (`V{version}__{description}.sql`)
3. 로컬에서 테스트
4. DTO 및 관련 코드 업데이트
5. 커밋 및 배포

---

## ⚠️ 주의사항

### Frontend

- ❌ `.env.local` 커밋 금지
- ✅ Server Actions에서 항상 인증 확인
- ✅ Zod 검증 필수
- ❌ `any` 타입 사용 금지

### Backend

- ✅ Flyway 마이그레이션 필수
- ❌ Production에서 직접 스키마 수정 금지
- ✅ DTO를 통한 요청/응답 처리
- ✅ `@ControllerAdvice`로 통합 에러 처리

### 데이터베이스

- ✅ 모든 테이블/컬럼은 snake_case
- ✅ UUID 기반 관계 설정
- ✅ Soft Delete 패턴 (deleted_at)
- ✅ 마이그레이션 파일 반드시 커밋

---

## 🔧 유용한 명령어

### Frontend 개발

```bash
pnpm dev              # 개발 서버 실행
pnpm build            # 프로덕션 빌드
pnpm lint             # ESLint 실행
```

### Backend 개발

```bash
./gradlew bootRun     # 개발 서버 실행
./gradlew build       # 빌드
./gradlew test        # 테스트
```

### 배포

```bash
vercel                # Frontend 배포 (Vercel)
# Backend는 Railway에서 자동 배포
```

---

## 🗄️ 데이터베이스 스키마 규칙

### 명명 규칙

- **테이블명**: snake_case (예: `family_members`)
- **컬럼명**: snake_case (예: `created_at`, `family_uuid`)
- **관계**: UUID 기반 (예: `family_uuid` → `families.uuid`)

### Entity 예시

```java
@Entity
@Table(name = "expenses")
public class Expense {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "uuid", unique = true, nullable = false)
    private String uuid;

    @Column(name = "family_uuid", nullable = false)
    private String familyUuid;

    @Column(name = "category_uuid", nullable = false)
    private String categoryUuid;

    @Column(name = "amount", nullable = false)
    private BigDecimal amount;

    @Column(name = "description", length = 1000)
    private String description;

    @Column(name = "date", nullable = false)
    private LocalDateTime date;

    @Column(name = "created_at", nullable = false)
    private LocalDateTime createdAt;

    @Column(name = "deleted_at")
    private LocalDateTime deletedAt;
}
```

---

## 🔄 최근 주요 개선사항

### Next.js 15 Server Actions 전환 (2025-09)

- API Routes → Server Actions 패턴으로 전환
- Zero JavaScript by Default
- 향상된 SEO 및 성능
- 타입 안전성 강화

### 타입 구조 개선 (2025-11)

- 도메인별 타입 파일 분리
- `src/types/actions/` 폴더 구조화
- 유지보수성 및 확장성 향상

### Zod 검증 도입

- 모든 Server Actions에서 Zod 스키마 검증
- 런타임 타입 안전성 보장
- 명확한 에러 메시지 제공

### SubmitButton 컴포넌트

- `useFormStatus` Hook 활용
- 중복 제출 자동 방지
- 로딩 상태 자동 표시

---

## 💡 핵심 원칙

1. **Server Components 우선**: 기본은 Server Component
2. **타입 안전성**: TypeScript + Zod 이중 검증
3. **도메인 분리**: 타입과 액션을 도메인별로 구조화
4. **API 통신 최소화**: Server Actions로 직접 백엔드 호출
5. **일관된 패턴**: 모든 기능에 동일한 패턴 적용
6. **에러 처리**: 명확한 에러 메시지와 처리
7. **UX 개선**: 중복 제출 방지, 로딩 상태 표시

---

## 🧪 테스트 작성 가이드

### 테스트 전략

**테스트 피라미드 비율:**
- Unit Tests (70%): 순수 함수, 유틸리티 테스트
- Component Tests (20%): React 컴포넌트 렌더링 및 상호작용
- Integration Tests (8%): Server Actions, API 통신
- E2E Tests (2%): 크리티컬 사용자 플로우

### 모킹 전략

```typescript
// ✅ 프론트엔드 테스트는 백엔드 연결 없이 모킹만 사용
jest.mock('@/lib/server/api/client', () => ({
  serverApiClient: jest.fn(),
}));

// ✅ Next.js 의존성 모킹
jest.mock('@/lib/server/auth-helpers');
jest.mock('@/lib/server/cookies');
jest.mock('next/cache', () => ({
  revalidatePath: jest.fn(),
}));

// ❌ jose 패키지는 직접 import 금지 (ESM 오류)
// auth 대신 auth-helpers를 모킹
```

### 테스트 작성 패턴

#### 1. Unit Test - 유틸리티 함수

```typescript
// src/__tests__/lib/utils/format.test.ts
import { formatCurrency } from '@/lib/utils/format';

describe('formatCurrency', () => {
  it('숫자를 한국 원화 형식으로 변환한다', () => {
    // Given
    const amount = 50000;
    
    // When
    const result = formatCurrency(amount);
    
    // Then
    expect(result).toBe('₩50,000');
  });
});
```

#### 2. Component Test - React 컴포넌트

```typescript
// src/__tests__/components/ui/SubmitButton.test.tsx
import { render, screen } from '@testing-library/react';
import { SubmitButton } from '@/components/ui/submit-button';

jest.mock('react-dom', () => ({
  useFormStatus: jest.fn(),
}));

describe('SubmitButton', () => {
  it('pending 상태일 때 버튼이 비활성화된다', () => {
    // Given
    useFormStatus.mockReturnValue({ pending: true });
    
    // When
    render(<SubmitButton>제출</SubmitButton>);
    
    // Then
    expect(screen.getByRole('button')).toBeDisabled();
  });
});
```

#### 3. Integration Test - Server Actions

```typescript
// src/__tests__/actions/expense/update-expense-action.test.ts
import { updateExpenseAction } from '@/app/actions/expense/update-expense-action';

jest.mock('@/lib/server/auth-helpers');
jest.mock('@/lib/server/api/client');
jest.mock('next/cache');

describe('updateExpenseAction', () => {
  it('유효한 데이터로 지출 수정에 성공한다', async () => {
    // Given
    mockRequireAuth.mockResolvedValue(undefined);
    mockServerApiClient.mockResolvedValue({ data: { uuid: 'test' } });
    
    const formData = new FormData();
    formData.append('amount', '50000');
    
    // When
    const result = await updateExpenseAction(initialState, formData);
    
    // Then
    expect(result.success).toBe(true);
    expect(mockServerApiClient).toHaveBeenCalledWith(
      expect.stringContaining('/expenses/'),
      expect.objectContaining({ method: 'PUT' })
    );
  });
});
```

### 테스트 실행

```bash
# 전체 테스트
pnpm test

# Watch 모드 (개발 중)
pnpm test:watch

# 커버리지
pnpm test:coverage

# 특정 파일만
pnpm test format
```

### 주의사항

1. **백엔드 연결 금지**: 모든 테스트는 모킹 사용
2. **tsconfig.json 타입 설정**: `"types": ["@testing-library/jest-dom"]` 필수
3. **AAA 패턴 준수**: Arrange → Act → Assert
4. **Given-When-Then**: 명확한 테스트 시나리오
5. **jest.setup.js**: 모든 테스트에 공통 설정 적용

---

## 📚 중요 파일들

### Frontend

- `src/app/actions/` - Server Actions (도메인별)
- `src/types/actions/` - 타입 정의 (도메인별)
- `src/components/ui/submit-button.tsx` - 폼 제출 버튼
- `src/lib/server/` - 서버 유틸리티
- `.cursor/rules/rules.mdc` - 개발 규칙

### Backend

- `src/main/java/.../presentation/controller/` - REST API
- `src/main/java/.../application/service/` - 비즈니스 로직
- `src/main/java/.../domain/entity/` - 엔티티
- `src/main/resources/db/migration/` - Flyway 마이그레이션
